# 📦 CRUDS – Product Management System
## 📖 Overview
CRUDS is a simple web application built with **HTML, CSS, and JavaScript** to demonstrate basic **CRUD operations (Create, Read, Update, Delete)** for managing products.  
All data is stored locally in the browser, so no backend setup is required.

## ✨ Features
- Add new products  
- View products  
- Update existing products  
- Delete products  
- Search products  
- Display product images  

## Image Note
Product images are loaded from the `imgs/` folder.  
To display an image, enter a name that matches one of the image files inside `imgs` (without the extension).  
If the name does not match an existing file, the image will not appear.

## 🛠️ Technologies Used
- HTML  
- CSS  
- JavaScript (Vanilla)
- Browser LocalStorage 
